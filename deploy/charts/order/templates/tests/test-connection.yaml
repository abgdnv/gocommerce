apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "order.fullname" . }}-test-connection"
  labels:
    {{- include "order.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox:1.37
      command: ['wget']
      args: ['-q', '-O', '-', '{{ include "order.fullname" . }}:{{ .Values.service.httpPort }}/healthz']
  restartPolicy: Never
